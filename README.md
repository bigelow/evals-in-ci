# Evals in CI: A Minimal Working Example

This repo demonstrates running LLM evals as ordinary CI checks against a small
set of vendored Kubernetes manifests ([fixtures/](fixtures/)) — no framework,
no harness, just pytest. It accompanies an essay (link TBD).
